var azadd = function() {
	var az= $('<button class="btn btn-default btn-sm" id="az-button">Az Just Annoyed Me!</button>');
	az.on("click", function() {
		$api.send("/w az_bot [azannoy]")
	});
	$("#az-button").remove();
	$(".chatbuttons > .btn-group").append(az);
};
azadd()
